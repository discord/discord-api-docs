{
	"name": "discord-api-docs",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"engines": {
		"node": ">= 20.11.0"
	},
	"workspaces": ["tools"],
	"scripts": {
		"prepare": "husky",
		"lint": "npm run -w tools test:tables && npm run -w tools lint",
		"lint:fix": "npm run -w tools fix:tables && npm run -w tools lint:fix",
		"build": "npm run -w tools build"
	},
	"devDependencies": {
		"husky": "^9.1.7",
		"lint-staged": "^16.4.0"
	},
	"lint-staged": {
		"*.{md,mdx}": "npm run lint:fix",
		"*.{ts,tsx,js,jsx,json}": "bash -c 'npm run lint:fix && npm run build'"
	}
}
